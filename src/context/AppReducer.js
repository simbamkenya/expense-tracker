export default (state, action)=> {
    switch (action.type) {
        // case value:
        // case value:
        // case value:
        // case value:
    
        default:
            return state;
    }
}